# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## プロジェクトの状態

このリポジトリ（kakeibo-app）は現時点ではまだ空で、コードベースは存在していません。
プロジェクトの構成（フレームワーク、ディレクトリ構造、ビルド/テストコマンドなど）が固まり次第、このファイルを更新し、以下のセクションを充実させてください。

- よく使うコマンド（build / lint / test / 単体テストの実行方法など）
- コードベース全体のアーキテクチャ概要

## Git運用ルール

- **コードに変更を加えたら、その都度コミットしてGitHubにプッシュすること。** 変更を作業ツリーに置いたままにせず、都度リモートに反映する。
- コミットは変更内容ごとに分け、目的が分かるコミットメッセージを付けること。
- push前に必ず現在のブランチとリモートの状態を確認すること。
